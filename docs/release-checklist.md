# WorkTrace Release Checklist

This file is retained as a compatibility pointer. The canonical release
validation baseline is [`docs/release-validation.md`](release-validation.md).

Phase DG1 (Documentation Governance) consolidated the release acceptance
items into `docs/release-validation.md` as the single source of truth for
release validation. This stub exists only to avoid breaking existing
references and test paths; it no longer contains a full checklist, command
list, privacy acceptance items, or release blockers. For all release
validation work, read [`docs/release-validation.md`](release-validation.md).
