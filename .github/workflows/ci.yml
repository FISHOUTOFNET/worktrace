name: CI

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-CI-refs/pull/2/merge
  cancel-in-progress: true

jobs:
  cancel-stale-pr-run:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "stale PR CI cancelled"
