name: Repair pytest diagnostics

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: repair-pytest-diagnostics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  read-failures:
    name: Read pytest failures
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 5

    steps:
      - name: Print compact JUnit failures
        shell: pwsh
        run: |
          $path = "test-results/pytest-junit.xml"
          if (-not (Test-Path -LiteralPath $path)) {
            Write-Host "NO_JUNIT_FILE"
            exit 0
          }
          [xml]$document = Get-Content -LiteralPath $path -Raw
          $nodes = @($document.SelectNodes("//testcase[failure or error]"))
          Write-Host "FAILURE_COUNT=$($nodes.Count)"
          foreach ($node in $nodes) {
            $failure = $node.failure
            if ($null -eq $failure) { $failure = $node.error }
            $message = [string]$failure.message
            $text = [string]$failure.InnerText
            if ($message.Length -gt 500) { $message = $message.Substring(0, 500) }
            if ($text.Length -gt 1500) { $text = $text.Substring(0, 1500) }
            Write-Host "FAILED_CASE=$($node.classname)::$($node.name)"
            Write-Host "FAILED_MESSAGE=$message"
            Write-Host "FAILED_TEXT=$text"
          }
