# Development, test, and Windows packaging dependencies.
# Install on top of the shipping runtime dependencies through this file:
#     pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-timeout>=2.3,<3
pyinstaller>=6.0
